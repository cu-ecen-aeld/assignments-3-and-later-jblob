name: assignment-test

on:
  push:
    branches:
      - '*'

jobs:
  full-test:
    runs-on: self-hosted

    container:
      image: cuaesd/aesd-autotest:24-assignment7-buildroot
      options: -v /home/jblob/yocto-cache:/yocto-cache -v /home/jblob/yocto-build:/build

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Checkout submodules
        run: git submodule update --init --recursive

      - name: Install dependencies + locale + socat
        run: |
          apt-get update
          apt-get install -y locales python3-distutils gawk chrpath diffstat lz4 zstd unzip build-essential socat
          echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
          locale-gen

      - name: Setup builder user and workspace
        run: |
          useradd -m builder || true
          rm -rf build
          ln -s /build build
          mkdir -p build/conf
          chown -R builder:builder .
          chown -R builder:builder /build
          chown -R builder:builder /yocto-cache || true

      - name: Start port forward (10022 -> 2222)
        run: |
          (while true; do
             socat TCP-LISTEN:10022,fork TCP:localhost:2222
             sleep 1
           done) &

      - name: Configure Yocto with full cache support
        shell: bash
        run: |
          su builder -s /bin/bash -c "
            mkdir -p build/conf

            [ -f build/conf/local.conf ] || echo 'CONF_VERSION = \"2\"' > build/conf/local.conf

            # Clean previous entries
            sed -i '/DL_DIR/d; /SSTATE_DIR/d; /SSTATE_MIRRORS/d; /TMPDIR/d; /MACHINE/d' build/conf/local.conf

            # ✅ Download cache
            echo 'DL_DIR = \"/yocto-cache/downloads\"' >> build/conf/local.conf

            # ✅ Shared sstate cache (MOST IMPORTANT)
            echo 'SSTATE_DIR = \"/yocto-cache/sstate-cache\"' >> build/conf/local.conf
            echo 'SSTATE_MIRRORS = \"file:///yocto-cache/sstate-cache/PATH\"' >> build/conf/local.conf

            # ✅ Persistent build output
            echo 'TMPDIR = \"/build/tmp\"' >> build/conf/local.conf

            # Standard settings
            echo 'MACHINE = \"qemuarm64\"' >> build/conf/local.conf
            echo 'EXTRA_IMAGE_FEATURES = \"debug-tweaks\"' >> build/conf/local.conf
            echo 'INHERIT += \"extrausers\"' >> build/conf/local.conf
            echo 'EXTRA_USERS_PARAMS = \"usermod -p \\\"\\\" root;\"' >> build/conf/local.conf
          "

      - name: Run full test
        shell: bash
        run: |
          su builder -s /bin/bash -c "
            export LANG=en_US.UTF-8
            export LC_ALL=en_US.UTF-8
            export HOME=/home/builder

            # ✅ capture repo root BEFORE yocto messes with cwd
            ROOTDIR=\$(pwd)

            source poky/oe-init-build-env build

            bitbake-layers add-layer \$ROOTDIR/meta-aesd

            cd \$ROOTDIR
            ./full-test.sh
          "
